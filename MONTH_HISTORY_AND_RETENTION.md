# বিগত মাসের হিসাব + ডেটা রিটেনশন (পুরনো ডেটা স্থায়ীভাবে ডিলিট)

**Branch:** `arena/01a0af86-messkhata`
**পরিবর্তিত ফাইল:** `index.html`, `ui.js`, `ui.css` (১ লাইন), `sw.js` (+ এই ডকুমেন্ট)
**Firebase/localStorage:** path ও key অপরিবর্তিত (`messKhata`, `messAppDataMealKhata`)

---

## ১. বিগত মাসের হিসাব দেখার ব্যবস্থা (নতুন)

Report স্ক্রিনের **Previous Months** তালিকার যেকোনো মাসে ট্যাপ করলে নতুন **Month Accounts** স্ক্রিন খোলে:

* উপরে **◀ মাসের নাম ▶** — ধরে রাখা পুরো সময়ের মধ্যে আগে/পরে যাওয়া যায় (ডেটা না থাকা মাসেও)
* টাইল: মোট বাজার, মোট ফিক্সড, **সর্বমোট খরচ**, মোট মিল, **ওই মাসের মিল রেট**
* **সদস্যদের হিসাব** — প্রত্যেকের মিল, খরচ, জমা এবং **পাবে/দেবে** (ব্যাজ)
* **বিস্তারিত টেবিল** (fold) — মিল, মিলের খরচ, ফিক্সড ভাগ, মোট খরচ, বাজার জমা, ফিক্সড জমা, অবস্থা + নিচে সর্বমোট
* **WhatsApp-এ এই মাসের হিসাব শেয়ার** বাটন

মাসের সব সংখ্যা একই সূত্রে হয় (`computeMonthSummary()`), মাস অনুযায়ী ছেঁকে:

```
মিল রেট      = ওই মাসের বাজার ÷ ওই মাসের মিল
মাসের খরচ    = মিলের খরচ + ওই মাসের ফিক্সড খরচের ভাগ
মাসের জমা    = ওই মাসের বাজার জমা + ওই মাসের ফিক্সড জমা
পাবে/দেবে    = মাসের জমা − মাসের খরচ
```

কোনো নতুন হিসাব-নিয়ম যোগ হয়নি — শুধু মাস ধরে দেখা। মিল রেট, মিল, বাজার, ফিক্সড ও সেটেলমেন্টের মূল লজিক অপরিবর্তিত।

---

## ২. রিটেনশন নিয়ম — চলতি মাস + বিগত ৩ মাস

| | |
|---|---|
| **যা রাখা হয়** | চলতি মাস + তার আগের ৩টি মাস (মোট ৪ মাস) |
| **যা মুছে যায়** | তার চেয়ে পুরনো **মিল, বাজার ও ফিক্সড খরচের এন্ট্রি** — স্থায়ীভাবে |
| **কখন** | খাতা খোলার সময়ই **স্বয়ংক্রিয়ভাবে** (Firebase থেকে ডেটা এলেও) |
| **কোথায়** | এই ডিভাইসের localStorage **এবং** Firebase ডেটাবেস — দুই জায়গা থেকেই |
| **সেটিং** | `RETENTION_PREVIOUS_MONTHS = 3` (index.html) — বদলাতে চাইলে শুধু এই সংখ্যাটি |

* ফাংশন: `purgeOldData()` — cutoff (`retentionCutoffKey()`) থেকে পুরনো এন্ট্রি বাদ দেয়, তারপর
  `saveToLocalStorage()` → Firebase-এ `ref.set(messData)` (নতুন ডেটা সিঙ্ক হয়)।
* **তারিখহীন পুরনো (legacy) ফিক্সড রেকর্ড** — পুরনো রেন্ট/বিল থেকে মাইগ্রেট হওয়া এন্ট্রি — **মোছা হয় না**,
  কারণ ওগুলো তারিখহীন এবং খাতার চলতি মান। ওগুলো তালিকায় `পুরনো রেকর্ড` লেবেল নিয়েই থাকে।
* পুরনো ডেটা কোনো পুরনো ডিভাইস থেকে আবার সিঙ্ক হয়ে ফিরে এলেও **সাথে সাথে আবার মুছে যায়**
  (real-time listener-এ purge + প্রয়োজনে আবার `ref.set`)।
* কিছু মুছে গেলে ফায়ারবেস স্ট্যাটাস/টোস্টে জানানো হয় এবং **More → “ডেটা ধরে রাখার নিয়ম” কার্ডে**
  দেখা যায়: কোন মাসগুলো রাখা হয়, কিছু মুছলে কতটি এন্ট্রি মুছে গেছে।
* Report-এর নিচে নোট: “চলতি মাস + বিগত ৩ মাস রাখা হয়।”

### ডেটা ফ্লো (একবার খাতা খুললেই)

```
localStorage → migrateFixedCostData() → purgeOldData() → calculateMess()/render
Firebase     → migrateFixedCostData() → purgeOldData() → ref.set(পরিষ্কার ডেটা) → render
```

---

## ৩. যা অপরিবর্তিত

* মিল এন্ট্রি, মিল রেট = মোট বাজার ÷ মোট মিল, বাজার এন্ট্রি/ছক/শেয়ার
* সদস্য, ডিপোজিট ও সেটেলমেন্টের লজিক, ফিক্সড খরচের এন্ট্রি UI (Bazar-এর মতো)
* localStorage key, Firebase path/config, PWA install flow
* `sw.js` cache version `v5 → v6` (নতুন স্ক্রিন/লজিক সবার ডিভাইসে যাবে)

---

## ৪. যাচাই (headless jsdom — **৬৯/৬৯ চেক পাস**)

* **রিটেনশন:** ৬ মাসের সিড করা ডেটা বুট করলে চলতি + বিগত ৩ মাস টিকে থাকে; −4, −5, −6 মাসের মিল/বাজার/ফিক্সড
  localStorage থেকে মুছে যায়; legacy রেকর্ড টিকে থাকে; মোট যোগফল প্রত্যাশিত (৪৬ মিল, ৳৪০০৬ বাজার)
* **Firebase:** পুরনো ক্লাউড ডেটা লোড হলে বাদ পড়ে এবং পরিষ্কার ডেটা ক্লাউডে ফের লেখা হয় (stub-এ যাচাই)
* **মাস ভিউ:** মাসের মিল/বাজার/ফিক্সড/রেট/সর্বমোট ও প্রতি সদস্যের পাবে/দেবে হাতে হিসাব করে মেলানো;
  চলতি মাসের ডেটা মাস ভিউতে মেশে না; ◀ ▶ নেভিগেশন; WhatsApp রিপোর্টে মাসের নাম, মোট ও সদস্যদের নাম
* **রিগ্রেশন:** পুরনো ফিক্সড ডেটার মাইগ্রেশন-প্যারিটি (মিল রেট, ভাগ, ব্যালেন্স অপরিবর্তিত),
  নতুন ফিক্সড এন্ট্রি/এডিট/ডিলিট, empty state — সব আগের মতোই পাস
