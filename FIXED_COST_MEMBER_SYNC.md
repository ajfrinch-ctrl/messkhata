# Fixed Cost — নতুন সদস্য যোগ করলেই তাঁর নাম Fixed Cost অংশে দেখায় (বাগ ফিক্স)

**Branch:** `arena/01a0afcf-messkhata` (base: `feature/firebase-realtime` @ `664a3df`)
**পরিবর্তিত ফাইল:** `index.html`, `ui.js`, `sw.js` (+ এই ডকুমেন্ট)
**অপরিবর্তিত:** `ui.css`, `manifest.webmanifest`, `icons/`, Firebase config/path (`messKhata`), localStorage key,
মিল/বাজার/ফিক্সডের **কোনো হিসাবের নিয়মই বদলায়নি**

---

## ১. সমস্যা কী ছিল

Fixed Cost এখন বাজারের মতোই এন্ট্রি-ভিত্তিক, কিন্তু সদস্যের তালিকাটি কয়েকটি জায়গায় **বিচ্ছিন্নভাবে**
সাজানো হতো — তাই নতুন সদস্য যোগ করলে Fixed Cost-এ তাঁর নাম দেখা না-ও যেতে পারত।

| # | সমস্যা | কখন দেখা দিত |
|---|---|---|
| 1 | Fixed Cost শিটের **Paid By** চিপের তালিকা শুধু `updateDropdowns()` কল হলেই নতুন হতো | অন্য ফোনে/ট্যাবে সদস্য যোগ হলে, বা শিট খোলা অবস্থায় তালিকা বদলালে — Fixed Cost-এ পুরনো লিস্টই দেখাতো |
| 2 | তালিকা নতুন করার সময় **বাছাই করা সদস্যটি রিসেট** হয়ে প্রথম সদস্য বসে যেত | শিট খোলা থাকতে থাকতে সদস্য যোগ করলে খরচটি **ভুল সদস্যের** নামে জমা হওয়ার ঝুঁকি |
| 3 | পুরনো রেকর্ডে (`fixedCosts.rent` → `scope:'member'`) খরচটি কোন সদস্যের, তা **তালিকায় `—`** এবং **ছকের ওই সদস্যের ঘরে `—` (৳০)** দেখাতো — অথচ Report/মাসের হিসাবে তাঁর ভাগ দেখানো হতো | মাইগ্রেট হওয়া যেকোনো খাতায় ছকের যোগফল মোট কার্ডের সাথে মেলেনি |
| 4 | `addMember()` id বানাতো `mem_<Date.now()>` দিয়ে — দ্রুত দুইবার চাপলে **দুই সদস্যের id একই** হতো, ফলে তালিকায় একজনের নাম বাদ যেত / আরেকজনের নাম ডুপ্লিকেট হতো | মোবাইলে দ্রুত ট্যাপ করলে |

---

## ২. কী করা হয়েছে

### ক) সদস্য-তালিকার একটাই উৎস (single source of truth)

`index.html`-এ যোগ হয়েছে:

```js
memberList()      // messData.members — ক্রম অনুযায়ী, id ছাড়া/ডুপ্লিকেট id বাদ দিয়ে
memberNameOf(id)  // id → নাম
fixedOwnerOf(f)   // একটি ফিক্সড এন্ট্রি দেখানো হয় কোন সদস্যের নামে
```

`ui.js`-এর `members()` এখন **এই একই তালিকা** পড়ে। ফলে মিল, বাজার, ফিক্সড — তিন স্ক্রিনের সদস্য
তালিকা, পিকার, ছক ও রিপোর্ট সব জায়গায় **হুবহু একই লিস্ট, একই ক্রম** বসে:
কেউ বাদ যায় না, কেউ দুইবার আসে না।

### খ) Fixed Cost শিটের সদস্য-তালিকা সবসময় তাজা

* `renderPickers()` (বাজার + ফিক্সড দুটো পিকারই একসাথে) এখন **`renderAll()`-এর প্রথম ধাপ** —
  অর্থাৎ এন্ট্রি সেভ, সদস্য যোগ/ডিলিট, রিসেট, ক্লাউড সিঙ্ক — যে পথেই ডেটা বদলাক না কেন তালিকা আপডেট হয়।
* **শিট খোলার মুহূর্তেও** (`openFixedSheet()` / `openBazarSheet()`) তালিকা নতুন করে সাজানো হয় —
  মাঝপথে কিছু বদলালেও পুরনো লিস্ট দেখাবে না।
* `pageshow` (ব্যাক-টু-ফরোয়ার্ড ক্যাশ থেকে ফিরলে) ও `visibilitychange`-এও রিফ্রেশ হয় —
  অন্য ফোনে যোগ করা সদস্য সাথে সাথেই Fixed Cost-এ দেখা যায়।
* সদস্যের তালিকা সত্যিই বদলালে তবেই সদস্য-নির্ভর ছকগুলো (`renderMealsGrid`, `renderBazarGrid`,
  `renderFixedGrid`) আবার রেন্ডার হয় — প্রতিবার অপ্রয়োজনে দামি কাজ হয় না।
* `<select>` রিবিল্ড করার যুক্তিটি `fillMemberSelect()`-এ এক জায়গায় নেওয়া হয়েছে,
  **বাছাই করা সদস্যটি টিকে থাকে** (সদস্য মুছে না গেলে) — সমস্যা ২ সমাধান।

### গ) Fixed Cost-এর UI/স্টাইল = বাজারের হুবহু একই

* `#fixed-member-chips` ও `#bazar-member-chips` — একই `.chips`/`.chip` মার্কআপ, একই ফাংশন থেকে তৈরি;
  শিটের লেআউট (তারিখ → সদস্য চিপ → নাম → টাকা) আগে থেকেই একই ছিল, সেটিই এখন একই **কোড** থেকে আসে।
  `ui.css`-এ কোনো বদল নেই (কোনো নতুন স্টাইল যোগ করা হয়নি)।

### ঘ) পুরনো রেকর্ডের নাম + ছকের অমিল

* `fixedOwnerOf()` — এন্ট্রিটি **যিনি দিয়েছেন** (memberId) তাঁর নামে, আর পুরনো রেকর্ড হলে **যার নামে খরচটি**
  (`scope:'member'` + `chargeTo`) তাঁর নামে দেখানো হয়।
  → Recent Fixed Cost, সব এন্ট্রির টেবিল, WhatsApp-এ ফিক্সড তালিকা — সব জায়গায় সদস্যের **নাম** দেখায় (`—` নয়)।
* `renderFixedGrid()` — কলামগুলো এখন `memberList()` থেকে আসে (নতুন সদস্য যোগ করলেই কলাম চলে আসে),
  এবং এন্ট্রি ওই সদস্যের ঘরে বসে; প্রতিদিনের **“মোট”** কলাম এখন ওই দিনের **সব** এন্ট্রির যোগফল,
  তাই ছকের যোগফল সবসময় **সর্বমোট ফিক্সড খরচ** কার্ডের সাথে মেলে।
* সদস্যের বিস্তারিত স্ক্রিনের “সাম্প্রতিক ফিক্সড খরচ”-এ পুরনো ভাড়ার রেকর্ডও দেখায়।

> ⚠️ এগুলো **শুধু দেখানোর বিভাজন** — `computeFixedSummary()` / `computeMonthSummary()` / `calculateMess()`
> সহ কোনো হিসাবের ফাংশন, ফর্মুলা বা ডেটা স্ট্রাকচার বদলানো হয়নি।

### ঙ) id ডুপ্লিকেট প্রতিরোধ

`addMember()` এখন `mem_<timestamp>` id-ই রাখে, কিন্তু ওই idটি আগে থেকেই থাকলে শুধু একটি র‍্যান্ডম
সফিক্স যোগ করে (`mem_<timestamp>_ab12c`) — আগের ডেটা/ফরম্যাট অপরিবর্তিত, তবু দুই সদস্য কখনোই
একই id পাবে না, তাই তালিকায় কারও নাম বাদ যাবে না বা ডুপ্লিকেট হবে না।

### চ) ক্যাশ ভার্সন

`sw.js`: `meal-khata-v7` → **`meal-khata-v8`** — পুরনো ক্যাশে করা `ui.js`/`index.html`-এর মিশ্রণে
পুরনো (স্টেল) সদস্য-তালিকা দেখানোর ঝুঁকি ছিল; ভার্সন বাম্পে পুরনো ক্যাশ মুছে যাবে।

---

## ৩. যা ইচ্ছাকৃতভাবে অপরিবর্তিত

* ফিক্সড খরচের নিয়ম: **প্রতিটি এন্ট্রি সব সদস্যের মধ্যে সমান ভাগে**, **যিনি দিয়েছেন তাঁর অ্যাকাউন্টে পুরো টাকা জমা**
* মিল রেট = মোট বাজার ÷ মোট মিল (ফিক্সড খরচ মিল রেটে ঢোকে না)
* মাইগ্রেশন (`migrateFixedCostData`), রিটেনশন পার্জ (চলতি + বিগত ৩ মাস), এন্ট্রি Add/Edit/Delete প্রবাহ
* Firebase কনফিগ/পাথ, `ref.set(messData)` সিঙ্ক, localStorage key, WhatsApp শেয়ারের ফরম্যাট
* বাজার/মিল স্ক্রিনের কোনো UI বা লজিক (শুধু ওপরের শেয়ার-করা পিকার সিঙ্ক দুই সেকশনেই একসাথে প্রযোজ্য)

---

## ৪. যাচাই (headless jsdom)

টেস্ট হারনেস: `index.html` + `ui.js` আসল DOM-এ লোড করে বাটন-ক্লিক/শিট/ফর্ম ফ্লো চালানো হয়েছে।

**৭৫টি চেক, সব পাস** — ৯টি গ্রুপ:

1. Fixed Cost-এর সদস্য তালিকা = বাজারের তালিকা (একই চিপ মার্কআপ, একই ক্লাস, একই `<select>` অপশন, একই ক্রম)
2. নতুন সদস্য যোগ → চিপ, লুকানো select, ছকের কলাম — সবখানেই নাম; কেউ বাদ নেই, কারও ডুপ্লিকেট নেই
2b. শিট খোলা অবস্থায় সদস্য যোগ করলেও তালিকা তাজা **এবং** আগে বাছাই করা সদস্যটিই নির্বাচিত থাকে
3. সদস্য মুছে দিলে Fixed Cost-এর তালিকা/ছক/select থেকেও বাদ
4. হিসাবের নিয়ম: মোট = এন্ট্রির যোগফল, ভাগ = মোট ÷ সদস্যসংখ্যা, জমা = পুরো টাকা, ছকের সর্বমোট = মোট কার্ড,
   মাসের মোট খরচ = বাজার + ফিক্সড, মিল রেট অপরিবর্তিত, পাবে/দেবে-র যোগফল ০
5. Add / Edit / Delete — ডাবল-কাউন্ট নেই, সদস্য-তালিকা অটুট
6. পুরনো ডেটা মাইগ্রেশন: ৬টি রেকর্ড, ভাড়া ওই সদস্যের, বিল সবার সমান ভাগ, জমা ০;
   নতুন সদস্য যোগ করলে বিল ৪ জনে ভাগ (নিয়ম অনুযায়ী) এবং মোট অপরিবর্তিত
7. ডুপ্লিকেট id → ৩টিই ইউনিক চিপ; একই নামের দুই আলাদা সদস্য → দুজনের নামই একবার করে
8. কোনো রেন্ডার কল না করা পথেও (শুধু ডেটা বদল) শিট খোলার মুহূর্তে তালিকা তাজা
9. WhatsApp ফিক্সড তালিকায় সদস্যের নাম ও সঠিক মোট

**প্যারিটি (হিসাব অপরিবর্তিত প্রমাণ):** একই সিড-করা অবস্থায় `HEAD` (ফিক্সের আগের) বনাম এখনের
snapshot — মোট ফিক্সড, প্রতি সদস্যের ভাগ/জমা, টাইল, মাসের হিসাব, ব্যালেন্স শীট, ছক, পিকার — **০টি পার্থক্য**।
পুরনো-স্কিমার ডেটায় একমাত্র পার্থক্যই প্রত্যাশিত: ছকের সদস্য-কলামে পুরনো ভাড়ার অঙ্ক এখন দেখায়
(আগে `৳০`, অথচ সর্বমোট ৳৭৪০০ — অমিল), সর্বমোটসহ বাকি সব সংখ্যা হুবহু আগের মতোই।

সার্ভিস ওয়ার্কার: `sw.js`-এর cache version **`meal-khata-v8`**।
